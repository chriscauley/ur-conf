[uwsgi]
project = ur-conf
uid = chriscauley
base = /home/chriscauley/projects/%(project)

chdir = %(base)
virtualenv = %(base)/.venv
module = main.wsgi:application

master = true
processes = 1

socket = %(project).sock
vacuum = true