[uwsgi]
project = ur-conf
uid = chriscauley
base = /home/chriscauley/projects/%(project)

chdir = %(base)
home = %(base)/.venv
module = main.wsgi:application

master = true
processes = 5

socket = /run/uwsgi/%(project).sock
chown-socket = %(uid):www-data
chmod-socket = 660
vacuum = true